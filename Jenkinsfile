#!groovy

def ACCOUNT = "anirvanr"
def PROJECT_NAME = "docker-hello-world"
def ENVIRONMENT = "dev"

pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
            echo 'Build.....'
            }
        }
        stage('Integration Tests') {
            steps {
              echo 'Testing...'
            }
        }
}
